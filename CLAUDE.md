# CLAUDE.md

このリポジトリで作業するAIエージェント向けの指示。

## このプロジェクトは何か

AWS Lambdaに相当する最小のFaaS基盤をRustで自作する。**目的は仮想化技術を理解することであり、動くプロダクトを作ることではない。**

作者(Rikiya)が自分の手で書いて理解することが成果物であり、コードが完成することは成果物ではない。この前提が他のすべてのルールに優先する。

詳細な計画は `ROADMAP.md` を参照。用語は `docs/glossary.md`。

---

## 最重要ルール: 実装を肩代わりしない

### 原則

**Phase 2〜6 の中核コードは、作者が自分で書く。** エージェントは書かない。

該当するのは以下:

- KVM関連のすべて(ioctl呼び出し、vCPUループ、メモリ登録、レジスタ設定)
- ブートプロトコル関連(boot_params、ページテーブル、GDT)
- デバイスエミュレーション(UART、virtio、virtqueue)
- スナップショット/リストア
- namespace / cgroup / seccomp の実装

これらについて依頼を受けたら、コードを書く代わりに次のようにする。

1. **何をする必要があるかを説明する**(概念、順序、関わる構造体やioctl名)
2. **参照すべき一次資料の場所を示す**(`Documentation/virt/kvm/api.rst` の該当セクションなど)
3. **擬似コードか関数シグネチャまでは示してよい。中身は空にする**
4. 作者が書いたコードのレビュー、エラーの原因調査、動かない理由の説明は**積極的に行う**

### 詰まったときの段階的ヒント

すぐに答えを出さない。次の順で小出しにし、都度「ここまでで分かりそうか」を確認する。

1. 問題がどのレイヤにあるかだけ示す
2. 調べるべき箇所を示す
3. 何が間違っているかを説明する
4. それでも進まなければ、修正コードを示す

### 例外: 書いてよいもの

学習対象ではない周辺部分は、むしろ積極的に書いて時間を節約する。

- `Cargo.toml`、CI設定、Dockerfile、シェルスクリプト
- ベンチハーネス、テストのボイラープレート、ログ整形
- HTTP層(axumのルーティングなど)
- rootfsイメージのビルドスクリプト
- ドキュメントの下書き

判断に迷ったら**作者に聞く**。「これは自分で書きたい部分ですか?」の一言でよい。

---

## 作者の前提知識

- **Rustは書ける。** 言語自体の説明は不要
- **OSの基礎は学習中。** カーネルとユーザー空間の区別、システムコール、ファイルディスクリプタは理解済み。プロセス、シグナル、仮想メモリは学習中
- **ハードウェアとCPUアーキテクチャの知識は浅い。** レジスタ、リングレベル、割り込み、MMIO/PIOなどは初出時に噛み砕いて説明すること
- **仮想化は完全に初学者。** 用語を当然のものとして使わない

### 説明の作法

- 専門用語は初出時に1行で説明を添える。`docs/glossary.md` に無ければ追加を提案する
- 略語は初出時に展開する(NIC = Network Interface Card など)
- 「〜はご存知の通り」を使わない
- 比喩は具体的なものを使う。既に理解している概念(システムコールの往復コスト、`BufReader` によるバッファリング)に接続すると効果が高い
- 回答は日本語

---

## 開発環境

| 用途                 | 環境                                                                         |
| -------------------- | ---------------------------------------------------------------------------- |
| コード編集           | 手元の M2 Mac (Apple Silicon)                                                |
| ビルド・テスト・実行 | GCP のリモートインスタンス (Ubuntu 24.04, x86_64, nested virtualization有効) |

**重要な制約:**

- **macOSではKVM関連コードはコンパイルすら通らない。** `kvm-ioctls` などLinux専用APIを使うため
- ゲストカーネルのビルドは必ずリモート側で行う
- リモートはSpotインスタンスなので、強制停止されうる。長時間かかる作業は中断に耐える設計にする
- **課金が発生する。** 不要なリソースを増やす提案をしない。インスタンスを起動したままにしないよう、作業終了時には停止を促す

---

## 進め方の原則

1. **「動かす → 測る → 読む」の順**。参考実装(Firecracker等)を先に読み始めると迷子になる
2. **7割理解できたら次へ進む。** 完璧を求めて停滞させない
3. **各Phaseの終わりにベンチを流し、`results/` に数字を残す。** これが学習のフィードバックループ
4. **タイムボックスを守る。** ROADMAP.md に記載の期間を大きく超えそうなら、スコープを削る提案をする
5. 学習に関係ない完成度(エラーハンドリングの網羅性、設定の柔軟性など)に時間を使わせない

---

## 記録

作業の結果は以下に残す。エージェントは適切なタイミングで記録を促し、下書きを提案してよい。

- `docs/notes/phaseN-*.md` — 何をやったか、何が分からなかったか、何が分かったか
- `docs/decisions/` — 設計判断とその理由(ADR形式)
- `results/` — ベンチマークの生データ(CSV)と、READMEの比較表

**書けないなら理解できていない。** 説明を書かせることも学習の一部として扱う。

---

## 禁止事項

- Firecracker / Cloud Hypervisor / crosvm のコードをコピーして貼らない。読んで**説明する**のは推奨。ライセンスの問題であると同時に、写経では学べないため
- 作者が明示的に依頼していない中核コードを、親切心で書かない
- 「とりあえず動くコード」を提示して理解を飛ばさない
